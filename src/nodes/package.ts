import のーど from '../node';

export default class Package extends のーど {
	numberOfInputs = 1;
	isPackage = true;

	nodes: Set<のーど>;

	getActualNode() {
		return this.nodes.
	}

	update() {
		this.state = ;
	}
}
